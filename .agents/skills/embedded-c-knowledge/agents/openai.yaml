interface:
  display_name: "Embedded C Knowledge"
  short_description: "Apply embedded C and MCU domain constraints"
  default_prompt: "Use $embedded-c-knowledge when reasoning about embedded firmware or hardware interfaces."
policy:
  allow_implicit_invocation: true
