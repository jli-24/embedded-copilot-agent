# ESP32 Guru Meditation evidence

Preserve the complete panic text, exception type, register dump, Backtrace,
firmware build identifier, ELF or map file, and reproduction conditions. A decoded
source line is evidence; an undecoded address or exception class alone normally
supports several possible causes rather than one confirmed root cause.
